# TP8 C16 large-prefill capacity/performance candidate

Base: `50913c2f24`, original DeepSeek-V4-Flash, original checkpoint precision,
native AR, TP8/EP1, 1,048,576-token logical KV pool, 16 distinct real8K source
code prompts (131,069 input tokens). This is separate from the slower default-off
projection/collective determinism diagnostics; those are OFF in this trial.

## Motivation and scope

The old M36864 ceiling originated in a TP4 OOM experiment. Current TP8 uses
half-width expert shards and has more headroom. AIter's actual CK sorter packs
token IDs into24 bits (not16). The candidate raises the limit only for I256
expert shards, under `SGLANG_DSV4_DEBUG_TP8_CK_64K=1`. The helper and runner
consume the same row limit. TP4/I512 remains capped at36864; defaults and small
prefill/decode selectors are unchanged. Unexpected M above65536 is rejected
by the helper. This is an opt-in experiment, not a default rollout.

## Component result

Only physical GCD4, synthetic BF16 inputs, packed FP4 weights, nonconstant
E8M0 scales, Top-6; same CK/SwiGLU/FP32 stage2 configuration as production.
Compare two stages (32768 + remainder) with one joint stage, including sorting,
weight expansion, stage1/stage2 and final cast. Three ABBA cycles per fixture.

| Routing | M | Two-stage median | Joint median | Time reduction |
| --- | ---: | ---: | ---: | ---: |
| balanced | 65536 | 51.002 ms | 46.630 ms | 8.57% |
| balanced | 65533 | 51.089 ms | 46.680 ms | 8.63% |
| skewed | 65536 | 45.223 ms | 40.379 ms | 10.71% |
| skewed | 65533 | 45.390 ms | 40.341 ms | 11.12% |

Each compared full output is finite and elementwise exact on these fixtures.
Component peak PyTorch allocation is6.40 GiB. This is **not** the full-model
VRAM footprint or proof of arbitrary CK atomic determinism. Three CPU guard
tests passed, including default behavior, TP4 exclusion and no GPU init.

## Completed real-service pilot

The service loaded successfully with chunk/max-prefill65536 and the full1M
KV pool. All eight ranks hit the large CK path. It executes two large prefill
forwards per C16 wave rather than four32768-sized forwards. France answered
Paris. No OOM or scheduler failure occurred; the harness stopped its service.

Warmup:4928.10 input tok/s (excluded). Three warm waves:
5407.09 / 5407.25 / 5315.29, median **5407.09 input tok/s**. This is streaming
batch TTFT throughput, not decode throughput or isolated GPU-forward timing.
Historical C16x8K32768 was approximately5310, but the observed variation is
close to this apparent difference: a fresh-process ABBA is required.

Two separate quality waves used all16 real prompts and128 output tokens.
All32 full prompt-ID echoes matched, cached-token counts were zero, outputs
had the expected lengths; the two sets of completion IDs matched16/16.
Four inspected openings were readable code-review responses. This is not a
whole-model numerical/semantic oracle and does not negate earlier row-placement
drift under a different request composition.

## Next gate

`abba.py` runs fresh A1 (32768), B1/B2 (65536), and A2 (32768), three timed
waves per leg and the same separate quality checks. Compare leg medians,
actual forward shapes, input identity, TTFT distribution and capacity.
Do not promote the candidate based only on the component or pilot.

Pilot evidence: `pilot-evidence.tar.gz`,26 files,784,892 bytes, SHA256
`ed78429ba08159655f5c053529f99f447a34238caed49ebe2b0abb82b6744388`.

## Completed fresh-process ABBA

Tested implementation: `b527352cf3`; a concurrent report-only commit did not
change the serving implementation. Three timed waves per leg, warmup excluded.
All arms used identical manifests and the same helper SHA256. Every timed
request generated one token, with zero cached tokens. Input throughput means
131,069 input tokens divided by the time from earliest request start to latest
first-token response. It is not resident decode throughput or pure GPU timing.

| Leg | Chunk | Median input tok/s | Median wave seconds | Median request TTFT seconds |
| --- | ---: | ---: | ---: | ---: |
| A1 | 32768 | 5296.750 | 24.7452 | 15.5531 |
| B1 | 65536 | 5395.896 | 24.2905 | 18.3131 |
| B2 | 65536 | 5394.960 | 24.2947 | 18.3191 |
| A2 | 32768 | 5300.998 | 24.7253 | 15.5524 |

Mean of the two leg medians: **5298.874 -> 5395.428 input tok/s,
+1.8222%**. Controls reproduce across independent services. Log byte ranges
show exactly four M32768 forwards per control wave and two M65536 forwards
per candidate wave. All eight ranks selected the large-M CK implementation.
The logical KV pool remains 1,048,576 tokens; no OOM or scheduler exception
occurred. Do not sum the repeated process VRAM entries from AMD-SMI as device
memory: that report does not establish full-model peak allocation.

The trade-off is real: mean of per-leg median request TTFT rises from
**15.5528 to 18.3161 seconds (+17.7674%)**. Larger cohorts finish the entire
wave sooner but hold early requests until a larger forward completes. Keep
the candidate **opt-in for throughput-oriented C16 x8K**, not the global
default or a general latency improvement. C1, TP4 and decode selectors are
unchanged. Do not mechanically add this gain to the earlier 2.62% experiment;
the two comparisons used different fresh controls.

Quality is a separate untimed batched endpoint check: each service ran two
16-request, 128-output-token waves. All **96 full prompt-ID echoes** match
the manifest; all returned completion IDs decode exactly to the returned text.
France answered Paris in all three processes. Same-order repeat matches are
A1 **15/16**, B **16/16**, A2 **15/16**. Four inspected candidate openings
(cases 0,3,10,15) are readable code-review responses, not a complete semantic
oracle. Production still exhibits drift: the candidate's single matching
pair is not evidence of whole-model determinism. Projection/collective
determinism diagnostics and fixed-slot CK were OFF throughout.

`analyze.py` validates the completed arms and emits `summary.json`, including
all 12 individual rates, input/source hashes, actual forward counts and the
latency trade-off. The three CPU capacity guard tests passed again. Every
owned service was stopped by its harness; ABBA process 998110 exited normally.

Final evidence: `abba-evidence.tar.gz`,75 files,2,347,811 bytes, SHA256
`fafb949bb5b61abe45c6bec5f6ad1b301fc39374435f2fb5d9890ce316c44b7a`.
The archive contains raw per-arm requests/results, launchers, service logs and
lifecycle records; the analyzer and summary are committed alongside it.

Next correctness work remains the independent frozen-input oracle for wo_b
and shared-expert row-placement sensitivity after the guarded QKV/wq_b repairs.
Those slower repairs have not been promoted or counted as throughput gains.
