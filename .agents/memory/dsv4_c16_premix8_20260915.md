# MHC pre-mix4->8: component pass, service ABBA pending

Scope: original V4, native TP8 C16 prefill, original precision and1M logical
KV. Latest accepted query16/runtime-M checkpoint is about6878 input tok/s.
Updated profile places the two pre-mix boundaries at3.060s/wave; that is the
remaining budget, not the old4.957s value.

## Implemented bounded oracle

Eight explicit accumulators,16 K1024 chunks, FP32 Fn and arithmetic, same
per-row sum/RMS expressions. Every load/store has the corresponding ragged
row mask. No FP16/BF16 Fn conversion, cross-row reduction, extra workspace or
LDS publication. The production four-row kernel remains unchanged.

`.agents/experiments/dsv4_c16_premix8_20260915/{candidate.py,screen.py}`
compares complete pre-mix kernels including RMS-partial consumption, not only
the dot loop. Residuals are repeated/scaled real captured rows, a synthetic
occupancy fixture, not fresh large-M full-model captures. Timings use three
ABBA cycles with five calls/event sample. Both output buffers are preallocated.

Initial M32768:8.84496->7.64361ms. Full repeat:8.84008->7.62069ms
(about13.8% less time,16.0% greater component throughput; these are different
percentage conventions). M8192:2.20164->1.89338ms; M65536:17.41853->15.26862ms.
Small M17/M128 regressed substantially and are deliberately excluded from
the eight-row wrapper selector.

900 randomized mutations passed bitwise, finite-output and sentinel checks:
100 each at M17/128/8192/32765/32766/32767/32768/65535/65536, changing Fn,
activations and epsilon. Row permutations passed for both paths at every M.
M128 candidate also passed1000 fixed graph replays. This does not establish
whole-model determinism or untested input domains.

M32768 compiler resources: control67 registers, candidate51; both0 spills,
0 LDS. M65536:68 versus52. Resources are from the recorded epsilon in each
result; the updated driver records both resource and timed epsilon explicitly.

## Default-off integration

`SGLANG_DSV4_PREFILL_MIX_GROUP_SIZE=8` selects the new kernel only inside the
existing native-prefill mix-reuse scope, K1024 and M8192..65536. Default remains
4. The helper also guards this M interval independently; lower/higher M fall
back to four rows. Existing TP8/V4/CP/TBO/speculative guards remain in force.
All-rank MHC first-hit logs now report actual group4/8.

The actual `gfx90a_mhc_pre_mix_from_partials_triton` dispatcher, context and
allocating helper were separately tested at M8192/32765/32768/65536 with10
randomized mutations each: all40 bitwise exact. Integrated M32768 medians
8.86634->7.65841ms. Preserve the12.283ms candidate first sample as a transient
outlier, not an explained-away hardware fault; other candidate samples were
7.638..7.721ms. This integrated test randomizes Fn between sizes, unlike the
component screen's reset-to-captured-Fn timing fixture.

CPU tests:28 passed,47 subtests, one unrelated asyncio_mode warning. New tests
check group selection, small/large boundaries, dtype fallback, context reset
and nondefault K fallback. Service launcher flag test confirms the only A/B
environment change is mix-group4 versus8, with query16/runtime1 fixed.

## Pending serving gate

Fresh sweep `.agents/experiments/dsv4_c16_premix8_service_20260915/` launched:
A1/B1/B2/A2, three waves/leg,131069 input IDs from16 real8K code tasks, zero
cache hits,1M KV, chunk32K. France plus two128-token quality waves/service,
input echo, source hashes, completion decoding and all-rank group hits are
retained. No default change or end-to-end gain is claimed before completion.
The service starts only after the standalone GPU4 tests end and all GPUs are
checked free. Do not run another GPU oracle concurrently with this TP8 sweep.

Full-model drift remains a separate unresolved item; prior runtime-M ABBA
had an A1-only case4 wording divergence after33 tokens with identical inputs.

Component archive:9 files,11878 bytes; SHA256
`be433882994eaa4b261b453f6215bd549c4af2fab87e4495059156368fcd7596`.
Tested candidate and integrated production kernel function ASTs are identical
(module docstring differs). Service A1 has since completed and stopped cleanly:
6878.46/6870.97/6863.52 input tok/s, median6870.97. Candidate B is running,
so there is still no accepted E2E gain or default promotion at this checkpoint.
