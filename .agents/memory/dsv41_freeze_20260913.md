# DeepSeek V4.1 Flash frozen at user request

Date: 2026-09-13, Asia/Hong_Kong. Previous committed checkpoint:
`bba7139b28` on `sync/media-foundry-gfx90a`.
Freeze branch: `backup/dsv41-frozen-20260913`.

The user explicitly ended V4.1 optimization, requested a frozen checkpoint
and service termination, and selected GLM5.3 Spark as the next project.
This is a research checkpoint, NOT a claim of completed numerical repair,
production readiness, optimized performance, or 1M-context support.
Do not resume V4.1 experiments unless the user explicitly requests it.

## Shutdown verified

The last diagnostic service was parent PID460976 with scheduler PIDs
462075--462082 and detokenizer462083, tmux `dsv41-router-20260913`,
log `/tmp/dsv41-router-20260913.log`, listening on port30101.
Its command line was verified as the V4.1 TP8 service before SIGTERM.

At22:38 HKT, parent and recorded child processes no longer existed,
`ss -ltnp 'sport = :30101'` showed no listener, and `amd-smi process`
reported no running processes on any GPU0--7. `amd-smi metric --mem-usage
--csv` reported used_vram10 and total_vram65520 in its MB units on every
GPU. The user's unrelated `model` tmux session was not terminated.
No model weights, caches, unrelated source changes, or diagnostics were deleted.

## Numerical acceptance at freeze

- Original checkpoint precision and host-backed Engram tables are retained.
- WO_A, attention, HC statistics, Engram gate, and the new router invariant
  selectors all remain default-off. The last service enabled them explicitly
  as a diagnostic configuration, not a new production default.
- Four-selector HC/Engram candidate: first cached step at fixed prefix204
  matches recompute Top20 logprobs exactly, with all164 captured current-row
  module outputs exact on rank0. This is not an all-prefix/all-rank proof.
- Its longer SQL answer has702 completion tokens, natural EOS, and passes
  16/16 event-sessionization fixtures. France returns Paris.
- Denser validation still finds a committed-token mismatch at prefix214
  (cached832 vs recompute34396), and the first observed logprob difference
  is at209. Sparse24/24 committed-ID matches do not override that failure.
- Historical first203-row tracing localizes the first difference to layer8
  MoE row71. A raw-checkpoint router replay has M-dependent logits and route
  weights consistent with that result. Actual runtime router/TopK outputs
  were not captured; do not call this replay a complete runtime oracle.
- New default-off router candidate passes common-prefix component tests,
  100 mutations across five M shapes and1000 individually checked graph
  replays. Earlier full unit run:128 tests plus16 subtests passed.
- Its new service reached readiness at22:29 but no test request was sent
  before the user's freeze. Router-candidate whole-model E2E remains UNTESTED.

Reports and request JSON are under
`../experiments/dsv41_hc_boundary_20260913/`. The large raw tensor traces
remain in their documented `/tmp/dsv41-*` directories and are not included
in Git. Reproduction scripts and compact numerical evidence are committed.

## Memory and speed: scope and limitations

Last service: TP8/EP1/no-A2A, original weights, Engram in pinned host RAM,
native AR, eager/no graphs, chunk2304, pool/context32768,
max_running_requests256, swa_full_tokens_ratio0.65.

The current startup audit, before KV allocation, reports per GCD:

| Quantity | GiB |
| --- | ---: |
| Unique named GPU model storage |53.987|
| Routed FP4 weights (included above) |42.188|
| Routed E8M0 scales (included above) |2.930|
| GPU Engram table storage |0.000|
| Host Engram mmap storage |23.604|
| Allocator live allocation |54.188|
| Allocator reserved after empty_cache |59.457|
| Inactive reserved after empty_cache |5.269|
| Device used after empty_cache |60.359--60.422|

The loading audit also observed device used63.156--63.219GiB before
empty_cache; these snapshots are not continuous full-run peak sampling.
Do not add aliases, component subtotals, reserved space, or per-process
peer-visible GPU listings as independent live allocations.

Current AIter local expert intermediate288 is padded to384. Existing
storage audit attributes11.499GiB/GCD versus the unpadded E8M0 layout to
geometry plus legacy W2 scale-axis padding, not FP32 scale expansion.

Logged KV coefficient is16814bytes/full-token; at1048576 tokens this is
16.4199GiB/GCD before page/fixed-buffer details. Current live model
allocation plus that estimate is70.6079GiB, beyond63.984GiB device capacity.
Using swa_full_tokens_ratio0.10 would yield3966bytes/token and3.873GiB
for the same pool by the current formula. Removing expert padding would
also materially change the budget. Neither alternative was implemented or
capacity-tested here; pool allocation is not proof of full1M prefill/decode
support, and lower SWA capacity requires admission/cache correctness tests.

Status-report estimates came from correctness HTTP requests, not formal
separated P/D benchmarks: C1 decode around5--6 output tokens/s
(702/128.6561s approximately5.46, including prefill), and short full-prefix
requests roughly200--275 input tokens/s. Tracing, eager execution, workload
length, and cold paths confound performance. Do not compare these to the old
V4 Flash optimized DSpark/native-AR throughput as matched measurements.

## Checkpoint boundaries

This freeze includes the pending V4.1 router candidate, bounded capture-call
test, scripts, reports, and the truthful incomplete-acceptance record.
Unrelated dirty `cuda_graph_runner_memory_usage.pickle` and older unrelated
untracked work are preserved outside this commit. No new GPU test is run
after the user's stop request. GLM5.3 Spark work must be separately scoped;
no GLM weights have been loaded or modified as part of this freeze.
